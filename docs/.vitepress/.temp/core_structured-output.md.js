import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Structured output","description":"","frontmatter":{},"headers":[],"relativePath":"core/structured-output.md","filePath":"core/structured-output.md"}');
const _sfc_main = { name: "core/structured-output.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="structured-output" tabindex="-1">Structured output <a class="header-anchor" href="#structured-output" aria-label="Permalink to &quot;Structured output&quot;">​</a></h1><p>Structured output turns a model response into a Go value. ai-go derives a strict JSON Schema, finds the first complete JSON value in the response, validates it locally, then decodes it into the requested type. Markdown fences and surrounding prose are accepted; invalid JSON and schema-invalid values are reported as typed errors.</p><p>This page follows the Extractors information architecture from Rig, but only describes ai-go behavior that is implemented today.</p><h2 id="pick-a-surface" tabindex="-1">Pick a surface <a class="header-anchor" href="#pick-a-surface" aria-label="Permalink to &quot;Pick a surface&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Best for</th><th>Returns</th></tr></thead><tbody><tr><td><code>agent.RunObject[T]</code></td><td>One Agent/Runner invocation</td><td><code>ObjectResult[T]</code>, including the full <code>Result</code></td></tr><tr><td><code>extractor.New[T](model).Build()</code></td><td>Repeated extraction using one prepared schema</td><td><code>extractor.Result[T]</code></td></tr></tbody></table><p>Use <code>ai.NewTypedCompletion[T]</code> for a simple request/response boundary; it is documented with <a href="/core/completions">Completions</a>, not as an extraction API. Use <code>RunObject</code> when the surrounding Agent lifecycle, transcript, hooks, tools, or step usage matter. Use <code>extractor.New[T]</code> when many inputs share the same target type and configuration.</p><h2 id="core-concepts" tabindex="-1">Core concepts <a class="header-anchor" href="#core-concepts" aria-label="Permalink to &quot;Core concepts&quot;">​</a></h2><p>Every structured-output surface uses one parse → validate → decode pipeline. The schema is derived once for <code>RunObject</code>; an <code>extractor.Extractor</code> retains it for its lifetime. <code>RunObject</code> replaces a schema previously configured on its Runner, so the Go type is the source of truth.</p><p><code>OutputMode</code> has <code>Auto</code>, <code>Native</code>, and <code>Tool</code> values. <code>Auto</code> resolves from the model capability before provider I/O. Native mode is the supported production path. The public Tool-mode resolver exists, but agent-level synthetic output tool interception is not implemented yet; do not force <code>OutputModeTool</code> in an application.</p><h2 id="provider-capability-matrix" tabindex="-1">Provider capability matrix <a class="header-anchor" href="#provider-capability-matrix" aria-label="Permalink to &quot;Provider capability matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Provider/model path</th><th>Native schema</th><th>With ordinary tools</th><th>Notes</th></tr></thead><tbody><tr><td>OpenAI Responses</td><td>Yes</td><td>Yes</td><td>Uses <code>text.format</code> JSON Schema</td></tr><tr><td>OpenAI Chat</td><td>Yes</td><td>Yes</td><td>Uses <code>response_format</code> JSON Schema</td></tr><tr><td>Gemini 3 (OpenAI-compatible or native)</td><td>Yes</td><td>Yes</td><td>Model-specific capability permits schemas with function declarations</td></tr><tr><td>Gemini before 3 (OpenAI-compatible or native)</td><td>Yes</td><td>No</td><td>Native schema cannot be combined with function declarations</td></tr><tr><td>Anthropic supported Claude 4.1/4.5+ IDs</td><td>Yes</td><td>Yes</td><td>Uses <code>output_config.format</code></td></tr><tr><td>Unknown or older Anthropic IDs</td><td>No</td><td>—</td><td>Fails locally with a structured-output error</td></tr><tr><td>Generic OpenAI-compatible model</td><td>Configured by provider</td><td>Depends on provider</td><td>Set the provider capability explicitly</td></tr></tbody></table><h2 id="target-type-requirements" tabindex="-1">Target type requirements <a class="header-anchor" href="#target-type-requirements" aria-label="Permalink to &quot;Target type requirements&quot;">​</a></h2><p><code>tool.StrictSchema[T]</code> supports exported struct fields, nested structs, arrays, numeric and boolean scalars, strings, and <code>enum</code> tags. Recursive types, maps, interfaces, and custom JSON decoders are rejected because they cannot produce a portable strict schema.</p><p>Every generated object has <code>additionalProperties: false</code>. Every property is listed in <code>required</code>; Go optionality is represented by accepting <code>null</code>:</p><table tabindex="0"><thead><tr><th>Go field</th><th>Strict-schema behavior</th></tr></thead><tbody><tr><td><code>Name string</code></td><td>Required string</td></tr><tr><td><code>Name *string</code></td><td>Required, string or null</td></tr><tr><td><code>Name string</code> with <code>omitempty</code></td><td>Required, string or null</td></tr><tr><td>Optional <code>enum:&quot;a,b&quot;</code> string</td><td>Required, <code>a</code>, <code>b</code>, or null</td></tr></tbody></table><p><code>tool.Schema[T]</code> is intentionally unchanged. It remains the schema used for tool inputs; use <code>StrictSchema[T]</code> only for output constraints.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><p>Runnable versions are in the sibling playground: run <code>go run ./examples/05-02-run-object</code>, <code>go run ./examples/05-03-extractor</code>, <code>go run ./examples/05-04-extractor-context</code>, <code>go run ./examples/05-05-extractor-errors</code>, or <code>go run ./examples/05-06-extractor-batch</code> from <code>test-ai-go</code>.</p><ul><li>Runner: build an <code>agent.Agent</code>, create a Runner with a prompt, then call <code>RunObject[T]</code>.</li><li>Extractor: call <code>extractor.New[T](model)</code>, configure it with value-builder methods, then call <code>Build</code> once and reuse <code>Extract</code> or <code>ExtractWithUsage</code>.</li></ul><h2 id="extractor-configuration-and-reuse" tabindex="-1">Extractor configuration and reuse <a class="header-anchor" href="#extractor-configuration-and-reuse" aria-label="Permalink to &quot;Extractor configuration and reuse&quot;">​</a></h2><p>The <code>extractor</code> builder has <code>Instructions</code>, <code>Context</code>, <code>Settings</code>, <code>ProviderOptions</code>, and <code>Retries</code>. Settings and provider options are applied to every attempt. <code>ExtractWithHistory</code> accepts caller-owned history plus the new input; it does not mutate that slice.</p><p>An extractor requires only decode-side compatibility from <code>T</code>; there is no encode-side constraint. It is not an Agent tool and must not be registered in a <code>tool.Set</code>.</p><h2 id="errors-and-validation" tabindex="-1">Errors and validation <a class="header-anchor" href="#errors-and-validation" aria-label="Permalink to &quot;Errors and validation&quot;">​</a></h2><p><code>llm.StructuredOutputError</code> is also re-exported as <code>agent.StructuredOutputError</code> and <code>ai.StructuredOutputError</code>. Its kinds are:</p><table tabindex="0"><thead><tr><th>Kind</th><th>Meaning</th></tr></thead><tbody><tr><td><code>prompt</code></td><td>Provider request could not be made or model capability is unsupported</td></tr><tr><td><code>empty</code></td><td>The provider returned no usable text</td></tr><tr><td><code>json_decode</code></td><td>No complete JSON value could be decoded</td></tr><tr><td><code>validation</code></td><td>JSON does not satisfy the output schema</td></tr></tbody></table><p>Use <code>errors.Is</code> for a kind and <code>errors.As</code> to inspect the error. <code>Extractor</code> returns <code>ExtractionError</code> after all attempts fail; it contains the final kind, attempt count, accumulated usage, and underlying cause. It retries output failures only, defaults to zero retries, and never retries a cancelled context or a prompt/provider failure.</p><h2 id="agent-results-streaming-and-ui" tabindex="-1">Agent results, streaming, and UI <a class="header-anchor" href="#agent-results-streaming-and-ui" aria-label="Permalink to &quot;Agent results, streaming, and UI&quot;">​</a></h2><p>For a normal final text turn, the Agent parses the existing response instead of issuing a dedicated finishing call. <code>MaxTurns: 1</code> is therefore sufficient for a tool-free structured response. A terminal tool turn can still need one constrained finishing call; that JSON is emitted only as <code>StepEventStructuredOutput</code>, never as <code>Result.Text</code> or a transcript message.</p><p>Structured output is server-side only today. Neither bundled protocol adapter maps <code>StepEventStructuredOutput</code> to a <code>useChat</code> data chunk, and partial objects are not streamed. Consume <code>Result.StructuredOutput</code> or the server-side event.</p><h2 id="best-practices" tabindex="-1">Best practices <a class="header-anchor" href="#best-practices" aria-label="Permalink to &quot;Best practices&quot;">​</a></h2><ul><li>Keep target types narrow and document fields with Go tags.</li><li>Prefer nullable fields over relying on a field being omitted.</li><li>Handle <code>ExtractionError</code> usage when retries are enabled: every attempt may be billed.</li><li>Preserve the returned <code>Result</code> or direct response for diagnostics, but do not log raw provider text without applying your data-handling policy.</li><li>Choose a provider/model from the capability matrix before combining output constraints with tools.</li></ul><h2 id="common-patterns-and-troubleshooting" tabindex="-1">Common patterns and troubleshooting <a class="header-anchor" href="#common-patterns-and-troubleshooting" aria-label="Permalink to &quot;Common patterns and troubleshooting&quot;">​</a></h2><p>For classification or batch work, build one <code>extractor.Extractor[T]</code> and call it for each document. For a single agent workflow, use <code>RunObject[T]</code> so transcript and tool results remain available beside the decoded value. For a single completion, use <code>ai.NewTypedCompletion[T]</code>.</p><p>If a result fails validation, first inspect the typed error kind and the provider response or Agent Result. An <code>empty</code> error usually means the model did not produce content; <code>json_decode</code> means no JSON value was present; and <code>validation</code> means the value did not satisfy the generated schema. A prompt error for Anthropic commonly means the selected model ID lacks native structured-output support.</p><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="/core/completions">Completions</a></li><li><a href="/core/agent-runner">Agent Runner</a></li><li><a href="/guides/error-handling">Error handling</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("core/structured-output.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const structuredOutput = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  structuredOutput as default
};
