import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"UI stream protocol extensions","description":"","frontmatter":{},"headers":[],"relativePath":"integrations/protocol-extensions.md","filePath":"integrations/protocol-extensions.md"}');
const _sfc_main = { name: "integrations/protocol-extensions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="ui-stream-protocol-extensions" tabindex="-1">UI stream protocol extensions <a class="header-anchor" href="#ui-stream-protocol-extensions" aria-label="Permalink to &quot;UI stream protocol extensions&quot;">​</a></h1><p><code>uistream</code> separates the event-driven transport mechanics from a UI protocol. It owns request/response framing, draining an <code>aikit.StepEvent</code> iterator, terminal-error normalization, flushing, and panic recovery. A protocol adapter owns its event vocabulary, ordering, block identity, persistence model, usage shape, and public error redaction.</p><p>Use <code>aisdkhttp.HandlerFor</code> to serve an adapter. The existing <code>Handler</code> remains the stable AI Node v7 compatibility default.</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">http.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Handle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;/chat&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, aisdkhttp.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">HandlerFor</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(agui.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Protocol</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(), run))</span></span></code></pre></div><p>The seam covers only event-driven streams. The <code>aisdk.Writer</code> and related imperative APIs intentionally remain AI Node-specific, since they can emit arbitrary protocol chunks that have no corresponding engine event.</p><p>The bundled <code>agui</code> adapter is a minimal subset: text and tool lifecycle events are supported; state synchronization, approvals, reasoning, source/file events, durability, and resume are deliberately not supported.</p><p>The adapter accepts the core <code>RunAgentInput</code> fields (<code>threadId</code>, <code>runId</code>, <code>messages</code>, <code>state</code>, <code>tools</code>, <code>context</code>, and <code>forwardedProps</code>) and emits <code>RUN_*</code>, <code>STEP_*</code>, <code>TEXT_MESSAGE_*</code>, and <code>TOOL_CALL_*</code> events. Usage snapshots are folded per step and returned under <code>RUN_FINISHED.result.usage</code>.</p><p><code>StepEventDone</code> is swallowed because the driver&#39;s <code>Finish</code> call owns the one terminal AG-UI event. Reasoning, structured-output, source, and file events are deliberately dropped. An approval request returns <code>agui.ErrToolApprovalUnsupported</code> and terminates with <code>RUN_ERROR</code>; AG-UI&#39;s interrupt/resume lifecycle is outside this minimal adapter.</p><p>This subset follows the official AG-UI core event and <code>RunAgentInput</code> documentation as reviewed on 2026-08-04. Behavioral conformance against a real AG-UI client remains a separate integration gate; the local golden tests prove JSON shape, ordering, pairing, and terminal behavior only.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("integrations/protocol-extensions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const protocolExtensions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  protocolExtensions as default
};
